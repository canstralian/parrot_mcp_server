apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mcp-data-pvc
  labels:
    app: mcp-server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
